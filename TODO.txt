todos:

introduce randomness in cat movement when there are multiple optimal choices

controls and stats

todo -better handling of start/stop/pause/reset training
event: dynamic cat (hiss)
prop-types
container layouts
better control view, support for parameter setting via ui
simplify game-state + move some logic to somewhere else

decay e value in e-greedy action selection over time